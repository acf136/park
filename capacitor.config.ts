import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.park2021.push',
  appName: 'park',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
